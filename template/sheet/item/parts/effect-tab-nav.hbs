{{!-- Shared Active-Effects tab button. Placed inside an item sheet's
      `.sheet-tabs` nav so every effect-bearing item exposes the same tab.
      The matching body lives in effect-tab.hbs. --}}
<b class="item" data-action="tab" data-group="primary" data-tab="effects">{{localize "DH.Effect.Effect"}}</b>
